{
	"private": true,
	"workspaces": [
		"language-service-plugin",
		"vscode-extension"
	],
	"devDependencies": {
		"vitest": "^1.6.0"
	},
	"scripts": {
		"build": "npm run build -w @vscode/ts-plugin-customized && npm run build -w ts-customized-language-service",
		"test": "npm run test -w @vscode/ts-plugin-customized"
	}
}